# Conexión al Backend con las APIs
# En navegador del host usamos localhost. Si el frontend corre dentro de un contenedor y necesita
# hablar con el host desde la red Docker, se puede usar http://host.docker.internal:3000.
VITE_API_BASE_URL=http://localhost:3000
VITE_FRONTEND_URL=http://localhost:5173