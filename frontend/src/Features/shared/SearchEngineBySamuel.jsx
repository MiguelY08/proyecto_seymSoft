// import { Search } from "lucide-react";

{/* <div className="relative w-full sm:w-90">
      <input
        type="text"
        placeholder="Buscar"
        value={search}
        onChange={(e) => setSearch(e.target.value)}
        className="w-full pl-5 pr-12 py-3 bg-white rounded-2xl border border-gray-300 shadow-md outline-none focus:ring-2 focus:ring-sky-900 text-black text-sm"
      />
      <Search
        size={20}
        className="absolute right-4 top-1/2 -translate-y-1/2 text-gray-600"
      /> */}

    //   Estilos del buscador